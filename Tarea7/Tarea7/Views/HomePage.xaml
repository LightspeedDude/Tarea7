<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Tarea7.Views.HomePage"
             xmlns:local="clr-namespace:Tarea7.Views">
    <ContentPage.Content>
        <StackLayout>

            <!--Passing Parameters-->
            <local:Display/>

            <!--Visual state-->
            <Entry IsEnabled="False" BackgroundColor="Black" Text="{Binding Givename}">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="Common">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="Lightblue" />
                            </VisualState.Setters>
                        </VisualState>

                        <VisualState x:Name="Focused">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="White" />
                            </VisualState.Setters>
                        </VisualState>

                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="Black" />
                            </VisualState.Setters>
                        </VisualState>
                        
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </Entry>

            <!--String format and Data Trigger-->
            <Label Text="{Binding Name, StringFormat='Oh, so you are {0}, nice to meet you'}"
                HorizontalOptions="CenterAndExpand">
                <Label.Triggers>
                    <DataTrigger TargetType="Label" Binding="{Binding Lucky}" Value="True">

                        <Setter Property="TextColor" Value="AliceBlue"/>
                    </DataTrigger>
                    <DataTrigger TargetType="Label" Binding="{Binding Lucky}" Value="False">

                        <Setter Property="TextColor" Value="Red"/>
                    </DataTrigger>
                </Label.Triggers>
            </Label>



        </StackLayout>
    </ContentPage.Content>
</ContentPage>