<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Tarea7.Views.Display">
  <ContentView.Content>
      <StackLayout>

            <!--Line break-->
            <Label Text="Welcome &#10; Do you know the time? &#128516;" FontSize="32" />
            <Image Source="clock" WidthRequest="120" HeightRequest="120" />
            
          <!--<Label Text="{Binding Day, StringFormat='{0:dd MM, yyyy}'}"/>-->

            <!--<Entry Placeholder="Give me your name">
                <Entry.Triggers>
                    <Trigger TargetType="Entry" Property="Isfocused" Value="True">
                        
                        <Setter Property="BackgroundColor" Value="AliceBlue"/>
                    </Trigger>
                </Entry.Triggers>
            </Entry>-->

            <!--<DataTrigger TargetType="Label" Binding="{Binding Lucky}" Value="False">

                <Setter Property="TextColor" Value="Red"/>
            </DataTrigger>-->

        </StackLayout>
  </ContentView.Content>
</ContentView>